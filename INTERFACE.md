# Interface do projeto

<!-- markdownlint-disable MD013 -->

Este arquivo é a fonte canônica para construir e reaproveitar a interface.
Atualize-o antes e depois de cada tarefa que criar ou mudar uma tela Svelte.
Leia `DESIGNSYSTEM.MD` antes de escolher a composição macro. Este arquivo
registra componentes, blocos e telas locais; as regras globais de SaaS vivem em
`DESIGNSYSTEM.MD`.

## Base observada

- Stack: SvelteKit 2.70.2 e Svelte 5.56.9
- Política: toda interface é composta por componentes Svelte.
- Primitives: shadcn-svelte local, com `Item` e `Sidebar` instalados em `apps/web`.
- Composições: nenhuma composição ReUI configurada no código atual.
- Tipografia: Geist Sans e Geist Mono locais em `apps/web/static/fonts/`.
- Direção visual: princípios de `https://vercel.com/design.md`, sem importar a
  identidade ou o shell da Vercel.

O uso de React, shadcn/ui ou ReUI não deve ser introduzido por suposição;
qualquer adoção futura precisa ser uma decisão explícita. Fonte escolhida para
shadcn-svelte: https://github.com/huntabyte/shadcn-svelte.

## Design system

| Item                       | Localização ou valor                    | Uso no projeto                                                                                  |
| -------------------------- | --------------------------------------- | ----------------------------------------------------------------------------------------------- |
| Tokens e tema              | `apps/web/src/app.css`                  | Cores, tipografia Geist, espaçamento, raio e tema claro/escuro                                  |
| Configuração shadcn-svelte | `apps/web/components.json`              | Estilo Nova, cor base Olive, aliases e registry                                                 |
| Composições Svelte         | `apps/web/src/lib/features/navigation/` | Seletor, shell, Sidebar, barra mobile, tema e status de rede                                    |
| PageHeader compartilhado   | `DESIGNSYSTEM.MD`                       | Um componente reutilizável para lista, detalhe, criação e edição                                |
| Padrão de dashboard        | `DESIGNSYSTEM.MD`                       | `PageHeader`, filtros, `KPI`, visualização principal e investigação detalhada                   |
| Padrão de linha            | `DESIGNSYSTEM.MD`                       | `DataGrid` em largura total, coluna `ID`, detalhe clicável por linha e ações de editar e apagar |
| Padrão de formulário       | `DESIGNSYSTEM.MD`                       | Seções, coluna de contexto e painel em duas colunas responsivas                                 |
| Padrão de contexto         | `DESIGNSYSTEM.MD`                       | `Breadcrumb` em todas as telas, com equipe, módulo e tela atual                                 |
| Primitives compartilhadas  | `apps/web/src/lib/components/ui/`       | Componentes locais copiados do shadcn-svelte                                                    |
| Composições de domínio     | `apps/web/src/lib/features/`            | Componentes próprios organizados por feature                                                    |

## Blocos criados e reaproveitáveis

Registre todos os blocos criados no projeto, inclusive os internos de uma
feature. Um bloco é um componente Svelte com responsabilidade própria, como
grade, formulário, filtro, cabeçalho, cartão, diálogo, painel lateral, estado
vazio, upload ou ação em lote.

| Bloco                 | Tipo                    | Arquivo                                                           | Origem                                                    | Finalidade e API pública                                                                                                                                                                                                   | Estados e acessibilidade                                                                                                                      | Consumidores                              | Reaproveitar ou estender                                                                               |
| --------------------- | ----------------------- | ----------------------------------------------------------------- | --------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| `Item`                | Primitive shadcn-svelte | `apps/web/src/lib/components/ui/item/`                            | shadcn-svelte local                                       | `Item`, `Item.Group`, `Item.Media`, `Item.Content`, `Item.Title`, `Item.Description`                                                                                                                                       | Links focáveis; superfície transparente; hover, seleção e estado disabled                                                                     | `/` e `/config` via `InitialScreenPicker` | Usar para opções e escolhas com título/descrição; manter composição local antes de criar card paralelo |
| `ButtonGroup`         | Primitive shadcn-svelte | `apps/web/src/lib/components/ui/button-group/`                    | shadcn-svelte local                                       | Agrupa ações compactas com `role="group"` e `data-slot="button-group"`                                                                                                                                                     | Ordem de teclado natural, foco em cada ação e agrupamento nomeado                                                                             | `BibleReader`                             | Usar para sequências de ações relacionadas; não transformar controles independentes em um botão único  |
| `Dialog`              | Primitive shadcn-svelte | `apps/web/src/lib/components/ui/dialog/`                          | shadcn-svelte local sobre Bits UI                         | Modal com portal, overlay, título, descrição e fechamento                                                                                                                                                                  | Foco gerenciado, fechamento acessível, `role="dialog"` e retorno de foco                                                                      | `BibleReader`                             | Usar para pesquisa e fluxos de foco no desktop; manter conteúdo curto e orientado à tarefa             |
| `Sidebar`             | Primitive shadcn-svelte | `apps/web/src/lib/components/ui/sidebar/`                         | shadcn-svelte local                                       | `Sidebar.Provider`, `Sidebar.Root`, `Sidebar.Content`, `Sidebar.Menu`, `Sidebar.MenuButton`                                                                                                                                | Sidebar expandido/recolhível no desktop, foco, tooltips e `aria-current` nos links                                                            | `AppSidebar` no shell global              | Usar como navegação desktop; a barra inferior é a navegação mobile da fatia                            |
| `InitialScreenPicker` | Composição de domínio   | `apps/web/src/lib/features/navigation/InitialScreenPicker.svelte` | Próprio com `Item`                                        | Props `mode` (`selector`/`config`) e `showHeading`; salva/remove preferência local                                                                                                                                         | Seletor vazio, escolha, disabled, erro e sucesso com `aria-live`; rádio acessível                                                             | `/` e `/config`                           | Reutilizar a composição para não divergir entre entrada e configuração                                 |
| `AppSidebar`          | Composição de domínio   | `apps/web/src/lib/features/navigation/AppSidebar.svelte`          | Próprio com `Sidebar`                                     | `collapsible="icon"` com ícones recolhidos por padrão; `Sidebar.Rail` na borda; labels visíveis ao expandir                                                                                                                 | Tooltips quando recolhido, rota ativa, foco, barra mobile com rótulos e `data-safe-area="bottom"`                                               | `AppFrame` via `Sidebar.Provider`         | `Sidebar.Trigger` no header desktop alterna abrir/fechar                                               |
| `ProductPage`         | Composição de domínio   | `apps/web/src/lib/features/navigation/ProductPage.svelte`         | Próprio                                                   | Props `title`, `description`, `status`, `statusTitle`, `statusBody`                                                                                                                                                        | Breadcrumb, estado vazio ou em breve, landmark `main`, retorno ao início                                                                      | `/sermons` e `/study`                     | Reutilizar até existirem módulos funcionais                                                            |
| `ThemeToggle`         | Composição de shell     | `apps/web/src/lib/features/navigation/ThemeToggle.svelte`         | Próprio com Lucide e `theme.ts`                           | Alterna `light`/`dark`, persiste em `openbible.theme`                                                                                                                                                                      | Label, título, foco visível e classe `.dark` na raiz                                                                                          | `AppSidebar` e header mobile              | Manter um único contrato de alternância nos dois shells                                                |
| `NetworkStatus`       | Composição de shell     | `apps/web/src/lib/features/navigation/NetworkStatus.svelte`       | Próprio com eventos de rede                               | Exibe o status offline sem bloquear conteúdo cacheado                                                                                                                                                                      | `role="status"`, `aria-live="polite"` e mensagem textual                                                                                      | `apps/web/src/routes/+layout.svelte`      | Usar como aviso não bloqueante; não prometer sincronização de dados                                    |
| `BibleReader`         | Composição de domínio   | `apps/web/src/lib/features/bible/BibleReader.svelte`              | Próprio com primitives shadcn-svelte e Lucide             | Props `storage`, `initialError` e `onRetry`; orquestra catálogo, seleção, capítulo, busca e CTA de importação                                                                                                              | Loading, indisponível, vazio com `Importar uma Bíblia`, erro, parcial, sucesso, `aria-live`, labels visíveis, foco, retry e retorno ao início | `apps/web/src/routes/bible/+page.svelte`  | Manter a página como coordenadora e concentrar o domínio em `bible-reader.ts`                          |
| `BibleReaderControls` | Bloco interno           | `apps/web/src/lib/features/bible/BibleReader.svelte`              | `ButtonGroup`, `Button`, `Dialog` e `Sheet` locais        | Duas pílulas: navegação (anterior, livro, capítulo, próximo) e ações (versão com abreviação, busca); seletores em modal desktop ou drawer mobile com título visível; versões em lista com badge e nome completo; livros em grade com busca; scrollbar oculto | Ordem de teclado, `aria-expanded` nos gatilhos, foco visível, botões disabled nos limites, seleção atual anunciada, `prefers-reduced-motion` e retorno acessível | `BibleReader`                             | Extrair apenas quando outra tela compartilhar a mesma seleção                                          |
| `BibleVerseList`      | Bloco interno           | `apps/web/src/lib/features/bible/BibleReader.svelte`              | Próprio com `<ol>` e itens semânticos                     | Renderiza número e texto dos versículos do capítulo atual                                                                                                                                                                  | Ordem de leitura, número em `font-mono`, quebra de texto longo e ausência de overflow                                                         | `BibleReader`                             | Extrair quando outro módulo precisar renderizar o mesmo contrato                                       |
| `AppShell`            | Composição de shell     | `apps/web/src/lib/features/workspace/AppFrame.svelte`             | Próprio com `AppSidebar`, `ThemeToggle` e `NetworkStatus` | Slot de conteúdo; mostra shell quando o workspace está `ready`                                                                                             | Sidebar desktop, header/barra mobile, tema, status offline e tela de permissão da pasta                                   | Rotas com workspace configurado           | Manter como shell único; onboarding e recuperação de permissão ficam fora do shell                 |
| `WorkspaceProvider`   | Estado de persistência  | `apps/web/src/lib/features/workspace/WorkspaceProvider.svelte`    | Próprio com `WorkspaceState`                              | Inicializa o workspace uma vez, pede persistência do origin e disponibiliza context                                                                        | loading, unconfigured, permission-needed, ready, error                                                                    | `+layout.svelte`                          | Única inicialização de storage; páginas não reabrem handle isoladamente                            |
| `PermissionRecovery`  | Composição de domínio   | `apps/web/src/lib/features/workspace/PermissionRecovery.svelte`   | Próprio                                                   | Recupera `queryPermission`/`requestPermission` da pasta local sem tratar o workspace como ausente                                                          | Permissão `prompt`/`denied`, cancelamento e nova escolha de pasta                                                         | `AppFrame`                                | Usar quando o handle existe mas a permissão voltou a `prompt`                                      |
| `ConfigPage`          | Composição de domínio   | `apps/web/src/lib/features/config/ConfigPage.svelte`              | `Tabs`, `WorkspaceSettings`, `InitialScreenPicker`        | Breadcrumb leve; desktop com abas linha (`Armazenamento`, `Tela inicial`); mobile com seções empilhadas e rótulos discretos; sem hero/título grande                                                                 | Tabs com foco visível, landmarks por seção, `prefers-reduced-motion` e painéis acessíveis por teclado                                          | `apps/web/src/routes/config/+page.svelte` | Manter `/config` como rota fina; concentrar layout aqui                                                |
| `Tabs`                | Primitive shadcn-svelte | `apps/web/src/lib/components/ui/tabs/`                            | shadcn-svelte local sobre Bits UI                         | Lista, gatilho e painel com variantes `default` e `line`                                                                                                                                                                   | `role="tablist"`, seleção por teclado, painel associado e foco visível                                                                        | `ConfigPage`                              | Usar variante `line` em configurações; reservar `default` para agrupamentos compactos                  |
| `WorkspaceSettings`   | Composição de domínio   | `apps/web/src/lib/features/workspace/WorkspaceSettings.svelte`    | Próprio                                                   | Prop `embedded` remove cabeçalho hero; mostra tipo, nome, status de Bíblias e persistência; ações de importar, escolher pasta e `persist()`                | Pronto, persistido, evictable, erro recuperável                                                                           | `/config` via `ConfigPage`                | Reutilizar para diagnóstico do workspace; não duplicar o onboarding                                |

## Telas e composição

| Tela ou rota                      | Arquivo                                                                  | Componentes Svelte usados                               | Dados e ações                                                                                                                                                                                                                                                                | Estados                                                                                                           |
| --------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| Workspace web                     | `apps/web/src/routes/+page.svelte`                                       | `OnboardingModal`, `InitialScreenPicker`, `ProjectHome` | Carregar configuração, orientar onboarding, redirecionar preferência e exibir entrada                                                                                                                                                                                        | Onboarding, seletor sem preferência, redirecionamento, erro, status de Bíblias e safe area                        |
| Entrada inicial                   | `apps/web/src/routes/+page.svelte`                                       | `InitialScreenPicker`, `Item`                           | Escolher Bíblia, sermão ou consultar estudo em breve                                                                                                                                                                                                                         | Sem preferência, links disponíveis e estudo disabled                                                              |
| Configuração                      | `apps/web/src/routes/config/+page.svelte`                                | `ConfigPage`, `Tabs`, `WorkspaceSettings`, `InitialScreenPicker` | Abas no desktop e seções no mobile; armazenamento, persistência, importação, pasta e tela inicial sem hero                                                                                                                | Pronto, persistido, evictable, erro, sem escolha e sucesso                                          |
| Bíblia                            | `apps/web/src/routes/bible/+page.svelte`                                 | `BibleReader`, `ButtonGroup`, `Dialog`, `Sheet`         | Barra em duas pílulas (navegação e versão/busca); selecionar livro em grade com busca, avançar para capítulo, escolher versão em lista, pesquisar em modal desktop ou drawer mobile, ler versículos, retomar e abrir importação quando vazio | Loading, vazio com CTA de importação, erro parcial/total, capítulo vazio, sucesso, busca sem resultados e limites |
| Sermões                           | `apps/web/src/routes/sermons/+page.svelte`                               | `ProductPage`                                           | Reservar a superfície do construtor futuro                                                                                                                                                                                                                                   | Estado inicial do módulo                                                                                          |
| Estudos                           | `apps/web/src/routes/study/+page.svelte`                                 | `ProductPage`                                           | Comunicar disponibilidade futura                                                                                                                                                                                                                                             | Estado explícito `Em breve`                                                                                       |
| PWA e instalação                  | `apps/web/static/manifest.webmanifest`, `apps/web/src/service-worker.ts` | Manifesto, app shell e service worker                   | Instalação standalone, cache versionado e fallback offline das cinco rotas locais                                                                                                                                                                                            | Primeiro carregamento online, cache disponível e indisponibilidade de rede                                        |
| Biblioteca de estudos (planejada) | A definir                                                                | Componentes Svelte a definir                            | Consultar, criar, editar e apagar estudos e sermões                                                                                                                                                                                                                          | A definir                                                                                                         |
| Construtor de sermões (planejado) | A definir                                                                | Componentes Svelte a definir                            | Estruturar, editar e salvar sermões                                                                                                                                                                                                                                          | A definir                                                                                                         |
| Leitor da Bíblia                  | `apps/web/src/lib/features/bible/BibleReader.svelte`                     | `BibleReader`, `BibleReaderControls`, `BibleVerseList`  | Consultar texto SQLite OpenLP importado, navegar, buscar e retomar seleção local                                                                                                                                                                                             | Loading, indisponível, vazio, erro, parcial, sucesso, busca e limites                                             |
| Notas (planejada)                 | A definir                                                                | Componentes Svelte a definir                            | Criar, consultar, editar e apagar notas                                                                                                                                                                                                                                      | A definir                                                                                                         |

### Onboarding de configuração e armazenamento

| Bloco               | Tipo                  | Arquivo                                                       | Origem                         | Finalidade e API pública                                                                                                                                     | Estados e acessibilidade                                                                                                                                     | Consumidores                       | Reaproveitar ou estender                                                                           |
| ------------------- | --------------------- | ------------------------------------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------- | -------------------------------------------------------------------------------------------------- |
| `OnboardingModal`   | Composição de domínio | `apps/web/src/lib/features/onboarding/OnboardingModal.svelte` | Próprio, com elementos nativos | Props `storageMode`, `storage`, `initialError`, `initialStep`, `onChooseStorage`, `onComplete` e `onDeferred`; pode abrir diretamente na etapa de importação | Intro, escolha, instalação, importação, erro, parcial e sucesso; `role=dialog`, foco visível, `aria-live`, trap de Tab e ações desabilitadas durante escrita | `apps/web/src/routes/+page.svelte` | Reutilizar a etapa de importação a partir do estado vazio do leitor sem duplicar upload ou storage |
| `ProgressIndicator` | Bloco interno         | `apps/web/src/lib/features/onboarding/OnboardingModal.svelte` | Elemento nativo `<progress>`   | Mostra progresso percentual da operação atual                                                                                                                | `aria-valuemin`, `aria-valuemax`, `aria-valuenow`, texto percentual e estado de operação                                                                     | `OnboardingModal`                  | Extrair somente quando outra operação assíncrona precisar do mesmo contrato                        |
| `BibleDropzone`     | Bloco interno         | `apps/web/src/lib/features/onboarding/OnboardingModal.svelte` | Input nativo e drag-and-drop   | Seleciona múltiplos arquivos `.sqlite` e encaminha arquivos escolhidos/soltos                                                                                | Operável por clique, Enter e Espaço; seleção vazia e resultados inválidos/parciais são anunciados                                                            | `OnboardingModal`                  | Extrair para um upload local compartilhado quando houver segundo consumidor                        |
| `ProjectHome`       | Composição de domínio | `apps/web/src/routes/+page.svelte`                            | Próprio                        | Mostra título do OpenBible, descrição e status `pending`, `partial` ou `complete` após o onboarding                                                          | Estado persistido, texto de status e layout responsivo sem depender apenas de cor                                                                            | `apps/web/src/routes/+page.svelte` | Substituir por shell do produto quando a biblioteca inicial existir                                |

## Regras de composição

1. Páginas e rotas Svelte coordenam dados e compõem componentes; não concentram a
   grade, formulário, filtros, overlays ou cartões reutilizáveis.
2. Antes de criar um componente, consulte esta tabela e reaproveite o item
   existente quando ele atender à mesma intenção.
3. Qualquer primitive ou composição adicionada entra na tabela com seu arquivo,
   origem, explicação, API, estados e consumidores reais.
4. A stack Svelte atual ainda não possui shadcn-svelte configurado; registrar
   cada primitive somente quando existir no projeto e tiver consumidor real.
5. Para dashboards, registre a pergunta operacional, escopo, filtros,
   indicadores, visualizações, tabela de investigação e estados. Prefira
   blocos existentes e primitives shadcn-svelte antes de criar uma nova
   composição.
6. Em CRUD, reutilize o mesmo `PageHeader` nas telas de lista, detalhe,
   criação e edição. Registre uma única implementação, suas props, variações e
   consumidores; não duplique markup de cabeçalho por página.
7. Linhas de `DataGrid` com detalhe usam link acessível em toda a área; a
   listagem ocupa a largura disponível, exibe a coluna `ID` e oferece ações de
   editar e apagar. Ações internas usam `TableRowAction` ou equivalente e não
   propagam a navegação.
8. Formulários de criar e editar usam seções com coluna de contexto e painel
   em duas colunas nos breakpoints largos, refluindo para uma no mobile.
9. Toda tela renderiza `Breadcrumb` com o nome da equipe ativa, o módulo e o
   título atual. Em Laravel, reaproveite o `Breadcrumb` ou `Breadcrumbs` do
   layout existente e registre o componente real em vez de criar outro.
10. Ao criar um bloco, registre-o nesta tabela na mesma tarefa. Ao alterar ou
    remover um bloco, atualize seus consumidores e a orientação de reuso.
11. Superfícies de navegação e itens de escolha começam transparentes; o hover
    pode usar `var(--muted)` ou `var(--sidebar-accent)`, enquanto seleção e foco
    usam borda, texto e semântica sem depender apenas de cor.
12. Use Geist Sans para a interface e Geist Mono somente para código, caminhos e
    identificadores técnicos. Não introduza decoração para preencher espaço.

<!-- markdownlint-enable MD013 -->
