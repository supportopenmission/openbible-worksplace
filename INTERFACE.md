# Interface do projeto

<!-- markdownlint-disable MD013 -->

Este arquivo é a fonte canônica para construir e reaproveitar a interface.
Atualize-o antes e depois de cada tarefa que criar ou mudar uma tela Svelte.
Leia `DESIGNSYSTEM.MD` antes de escolher a composição macro. Este arquivo
registra componentes, blocos e telas locais; as regras globais de SaaS vivem em
`DESIGNSYSTEM.MD`.

## Base observada

- Stack: SvelteKit 2.70.2 e Svelte 5.56.9
- Política: toda interface é composta por componentes Svelte.
- Primitives: shadcn-svelte local, com `Item` e `Sidebar` instalados em `apps/web`.
- Composições: nenhuma composição ReUI configurada no código atual.
- Tipografia: Geist Sans e Geist Mono locais em `apps/web/static/fonts/`.
- Direção visual: princípios de `https://vercel.com/design.md`, sem importar a
  identidade ou o shell da Vercel.

O uso de React, shadcn/ui ou ReUI não deve ser introduzido por suposição;
qualquer adoção futura precisa ser uma decisão explícita. Fonte escolhida para
shadcn-svelte: https://github.com/huntabyte/shadcn-svelte.

## Design system

| Item                       | Localização ou valor                    | Uso no projeto                                                                                  |
| -------------------------- | --------------------------------------- | ----------------------------------------------------------------------------------------------- |
| Tokens e tema              | `apps/web/src/app.css`                  | Cores, tipografia Geist, espaçamento, raio e tema claro/escuro                                  |
| Configuração shadcn-svelte | `apps/web/components.json`              | Estilo Nova, cor base Olive, aliases e registry                                                 |
| Composições Svelte         | `apps/web/src/lib/features/navigation/` | Seletor, shell, Sidebar, barra mobile, tema e status de rede                                    |
| PageHeader compartilhado   | `DESIGNSYSTEM.MD`                       | Um componente reutilizável para lista, detalhe, criação e edição                                |
| Padrão de dashboard        | `DESIGNSYSTEM.MD`                       | `PageHeader`, filtros, `KPI`, visualização principal e investigação detalhada                   |
| Padrão de linha            | `DESIGNSYSTEM.MD`                       | `DataGrid` em largura total, coluna `ID`, detalhe clicável por linha e ações de editar e apagar |
| Padrão de formulário       | `DESIGNSYSTEM.MD`                       | Seções, coluna de contexto e painel em duas colunas responsivas                                 |
| Padrão de contexto         | `DESIGNSYSTEM.MD`                       | `Breadcrumb` em todas as telas, com equipe, módulo e tela atual                                 |
| Primitives compartilhadas  | `apps/web/src/lib/components/ui/`       | Componentes locais copiados do shadcn-svelte                                                    |
| Composições de domínio     | `apps/web/src/lib/features/`            | Componentes próprios organizados por feature                                                    |

## Blocos criados e reaproveitáveis

Registre todos os blocos criados no projeto, inclusive os internos de uma
feature. Um bloco é um componente Svelte com responsabilidade própria, como
grade, formulário, filtro, cabeçalho, cartão, diálogo, painel lateral, estado
vazio, upload ou ação em lote.

| Bloco                 | Tipo                    | Arquivo                                                           | Origem                                                    | Finalidade e API pública                                                                                                                                                                                                   | Estados e acessibilidade                                                                                                                      | Consumidores                              | Reaproveitar ou estender                                                                               |
| --------------------- | ----------------------- | ----------------------------------------------------------------- | --------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| `Item`                | Primitive shadcn-svelte | `apps/web/src/lib/components/ui/item/`                            | shadcn-svelte local                                       | `Item`, `Item.Group`, `Item.Media`, `Item.Content`, `Item.Title`, `Item.Description`                                                                                                                                       | Links focáveis; superfície transparente; hover, seleção e estado disabled                                                                     | Removido da `/` em SPEC-0012 (`QuickActions` usa links e botões próprios) | Reaproveitar quando surgir nova escolha com título e descrição |
| `ButtonGroup`         | Primitive shadcn-svelte | `apps/web/src/lib/components/ui/button-group/`                    | shadcn-svelte local                                       | Agrupa ações compactas com `role="group"` e `data-slot="button-group"`                                                                                                                                                     | Ordem de teclado natural, foco em cada ação e agrupamento nomeado                                                                             | `BibleReader`                             | Usar para sequências de ações relacionadas; não transformar controles independentes em um botão único  |
| `Dialog`              | Primitive shadcn-svelte | `apps/web/src/lib/components/ui/dialog/`                          | shadcn-svelte local sobre Bits UI                         | Modal com portal, overlay, título, descrição e fechamento                                                                                                                                                                  | Foco gerenciado, fechamento acessível, `role="dialog"` e retorno de foco                                                                      | `BibleReader`                             | Usar para pesquisa e fluxos de foco no desktop; manter conteúdo curto e orientado à tarefa             |
| `Sidebar`             | Primitive shadcn-svelte | `apps/web/src/lib/components/ui/sidebar/`                         | shadcn-svelte local                                       | `Sidebar.Provider`, `Sidebar.Root`, `Sidebar.Content`, `Sidebar.Menu`, `Sidebar.MenuButton`                                                                                                                                | Sidebar expandido/recolhível no desktop, foco, tooltips e `aria-current` nos links                                                            | `AppSidebar` no shell global              | Usar como navegação desktop; a barra inferior é a navegação mobile da fatia                            |
| `HomePage` | Composição de domínio | `apps/web/src/lib/features/home/HomePage.svelte` | Próprio com `PageHeader`, `ContinueReadingCard`, `QuickActions`, `RecentLists` | Props `storage` opcional; carrega continuação, recentes e catálogo em paralelo com `home-continuation.ts` e `home-recents.ts` | Skeleton, vazio, erro com retry por seção, `aria-live`, foco visível, `prefers-reduced-motion` | `/` via `apps/web/src/routes/+page.svelte` | Estender seções sem duplicar a coordenação de carga |
| `ContinueReadingCard` | Composição de domínio | `apps/web/src/lib/features/home/ContinueReadingCard.svelte` | Próprio com `Button`, `Empty`, `Skeleton` | Props `continuation`, `catalog`, `loading`, `errorMessage`, `onRetry`; exibe seleção ou último destaque como `{livro} {capítulo}[.versículos] · {versão}` com link para `/bible`, ou CTA Abrir a Bíblia | Skeleton, vazio com CTA, erro com retry, `role="alert"`, nomes acessíveis | `HomePage` | Reutilizar o card em qualquer retomada de leitura |
| `QuickActions` | Composição de domínio | `apps/web/src/lib/features/home/QuickActions.svelte` | Próprio com Lucide | Atalhos Ler a Bíblia (`/bible`), Nova nota (cria e abre `/notes/[id]`) e Novo sermão (`/sermons`); erro de criação com `role="alert"` | Carrossel horizontal com snap no mobile, foco visível, ação disabled durante criação, `prefers-reduced-motion` | `HomePage` | Reutilizar atalhos sem recriar navegação por tarefa |
| `RecentLists` | Composição de domínio | `apps/web/src/lib/features/home/RecentLists.svelte` | Próprio com `HighlightsList` | Props `notes`, `highlights`, `catalog`, `storage`, erros, retries e `onCreateNote`; notas com link e data, destaques via `HighlightsList` (`layout="rail"`, `emptyVariant="inline"`) com navegação para `/bible` | Carrossel horizontal com snap no mobile, vazio minimalista em linha com ação, erro com retry, `role="alert"`, foco visível | `HomePage` | Reutilizar listas sem simular módulos futuros |
| `AppSidebar`          | Composição de domínio   | `apps/web/src/lib/features/navigation/AppSidebar.svelte`          | Próprio com `Sidebar`                                     | `collapsible="icon"` com ícones recolhidos por padrão; links Início (`House`, `/`), Bíblia, Notas, **Destaques** (`Highlighter`, `/highlights`), Sermões, Estudos e Configuração; `Sidebar.Rail` na borda                                                         | Tooltips quando recolhido, `aria-current="page"` em `/notes`, `/notes/[id]` e `/highlights`, barra mobile 5 colunas (Início, Notas, Bíblia, Sermões, Configuração; sem Destaques) com rótulos e `data-safe-area="bottom"`     | `AppFrame` via `Sidebar.Provider`         | Estender links sem duplicar barra mobile; `Sidebar.Trigger` no header desktop alterna abrir/fechar   |
| `ProductPage`         | Composição de domínio   | `apps/web/src/lib/features/navigation/ProductPage.svelte`         | Próprio                                                   | Props `title`, `description`, `status`, `statusTitle`, `statusBody`                                                                                                                                                        | Breadcrumb, estado vazio ou em breve, landmark `main`, retorno ao início                                                                      | `/sermons` e `/study`                     | Reutilizar até existirem módulos funcionais                                                            |
| `ThemeToggle`         | Composição de shell     | `apps/web/src/lib/features/navigation/ThemeToggle.svelte`         | Próprio com Lucide e `theme.ts`                           | Alterna `light`/`dark`, persiste em `openbible.theme`                                                                                                                                                                      | Label, título, foco visível e classe `.dark` na raiz                                                                                          | `AppSidebar` e header mobile              | Manter um único contrato de alternância nos dois shells                                                |
| `NetworkStatus`       | Composição de shell     | `apps/web/src/lib/features/navigation/NetworkStatus.svelte`       | Próprio com eventos de rede                               | Exibe o status offline sem bloquear conteúdo cacheado                                                                                                                                                                      | `role="status"`, `aria-live="polite"` e mensagem textual                                                                                      | `apps/web/src/routes/+layout.svelte`      | Usar como aviso não bloqueante; não prometer sincronização de dados                                    |
| `BibleReader`         | Composição de domínio   | `apps/web/src/lib/features/bible/BibleReader.svelte`              | Próprio com primitives shadcn-svelte e Lucide             | Props `storage`, `initialError` e `onRetry`; orquestra catálogo, capítulo, busca, seleção contínua de versículos, markup de highlights, sheet de destaques workspace-wide, ícone de nota com badge opcional (2–9/9+), seletor multi-nota, popover de ações e split/abas da nota ou grade filtrada | Loading, indisponível, vazio com `Importar uma Bíblia`, erro, parcial, sucesso, seleção, popover, sheet Destaques, ícone Abrir nota (1 nota direto; 2+ seletor), split/abas ou cards filtrados, clipboard/workspace recuperáveis, `aria-live`, labels visíveis, foco, retry e retorno ao início | `apps/web/src/routes/bible/+page.svelte`  | Manter a página como coordenadora e concentrar o domínio em `bible-reader.ts`, `reader-highlights.ts` e `reader-verse-notes.ts` |
| `HighlightsList`      | Composição de domínio   | `apps/web/src/lib/features/bible/HighlightsList.svelte`           | Próprio                                                   | Props `highlights`, `catalog` opcional, `layout` (`grid`/`rail`), `emptyVariant` (`panel`/`inline`) e `emptyMessage`; card minimal com referência em mono, versão e indicador de estilo Q6 para o conjunto workspace-wide de `reader_highlight`                                                                 | Lista vazia observável com `role="status"`; referência e estilo em hierarquia tipográfica discreta                                              | `BibleReader` (sheet), `/highlights`, `RecentLists` (home, `layout="rail"`) | Reutilizar a mesma lista no sheet do leitor, na página `/highlights` e na home; não navegar ao capítulo na linha |
| `BibleReaderControls` | Bloco interno           | `apps/web/src/lib/features/bible/BibleReader.svelte`              | `ButtonGroup`, `Button`, `Dialog` e `Sheet` locais        | Pílula de navegação (anterior, livro, capítulo, versão, próximo, busca, **Destaques**) e seletores em modal desktop ou drawer mobile com título visível; versões em lista com badge e nome completo; livros em grade com busca; scrollbar oculto | Ordem de teclado, `aria-expanded` nos gatilhos, foco visível, botões disabled nos limites, seleção atual anunciada, sheet Destaques com Escape, `prefers-reduced-motion` e retorno acessível | `BibleReader`                             | Extrair apenas quando outra tela compartilhar a mesma seleção                                          |
| `BibleVerseList`      | Bloco interno           | `apps/web/src/lib/features/bible/BibleReader.svelte`              | Próprio com `<ol>` e botões de versículo                  | Renderiza número, **ícone discreto de nota** (`Abrir nota`) com badge 2–9/9+ quando há 2+ notas distintas, e texto; clique/Shift forma intervalo contínuo; overlay une todos os highlights que cobrem o versículo                                                                                                    | `aria-pressed` na seleção, `sr-only` com nomes dos estilos, botão `Abrir nota` focável, badge monocromático, foco visível, Escape fecha o popover, overflow-wrap no texto                      | `BibleReader`                             | Extrair quando outro módulo precisar renderizar o mesmo contrato                                       |
| `VerseNoteSelector`   | Composição de domínio   | `apps/web/src/lib/features/bible/VerseNoteSelector.svelte`        | Próprio com `Button`, `Sheet` shadcn-svelte               | Props `open`, `anchor`, `mobile`, `summaries`, `loading`, `errorMessage`, `onSelectNote`, `onViewAll`, `onClose`; popover desktop ancorado ao ícone ou drawer mobile com títulos ordenados por `updatedAt` e ação **Ver todas** | `role="dialog"` (`Notas do versículo`), foco no primeiro título, Escape, loading/erro recuperável, `prefers-reduced-motion`, tema claro/escuro | `BibleReader`                             | Reusar padrão de posicionamento de `SelectionActionPopover`; sem vínculo com highlight (DEC-002)      |
| `SelectionActionPopover` | Composição de domínio | `apps/web/src/lib/features/bible/SelectionActionPopover.svelte` | Próprio com `Button` shadcn-svelte                        | Paleta Q6 (canetas nomeadas, sublinhado, ondulado, caixa, apagar), copiar referência, copiar texto e referência, criar nota; ancorado ao versículo, `position: fixed` com folga de viewport                                | `role="dialog"`, foco no primeiro botão, Escape, `role="alert"` em clipboard/workspace, `prefers-reduced-motion`, tema claro/escuro           | `BibleReader`                             | Não promover a painel permanente; não reutilizar o highlight TipTap da nota                            |
| `BibleNoteSplit`      | Composição de domínio   | `apps/web/src/lib/features/bible/BibleNoteSplit.svelte`           | Próprio com `Tabs` shadcn-svelte, `NoteCanvasEditor` e `NoteCardList` | Props `note`, `listNotes`, `hideListDelete`, `storage`, `onClose`, `onSaved`, `onSelectListNote` e snippet `children`; desktop em duas colunas, mobile com abas Bíblia/Nota; editor ou grade filtrada de notas do versículo, sem sair de `/bible` | Sem nota (só o reader), split desktop, abas mobile, grade filtrada com `data-testid="notes-card-list"`, título longo com ellipsis, `aria-label="Nota"`, `prefers-reduced-motion` | `BibleReader`                             | Reusar o editor da SPEC-0004; não criar rota que substitua `/bible`                                    |
| `AppShell`            | Composição de shell     | `apps/web/src/lib/features/workspace/AppFrame.svelte`             | Próprio com `AppSidebar`, `ThemeToggle` e `NetworkStatus` | Slot de conteúdo; mostra `WorkspaceBootSplash` quando `loading` e shell quando o workspace está `ready`; header desktop fixo com scroll interno das seções; sem header mobile — cada rota exibe seu próprio título e a barra inferior é a navegação | Sidebar desktop, títulos por página, barra mobile, tema, status offline e tela de permissão da pasta                                   | Rotas com workspace configurado           | Manter como shell único; onboarding e recuperação de permissão ficam fora do shell                 |
| `WorkspaceBootSplash` | Composição de shell | `apps/web/src/lib/features/workspace/WorkspaceBootSplash.svelte` | Próprio | Splash de boot com logo, barras em pulso, status e dica; `aria-busy` e `aria-live` | `prefers-reduced-motion` sem animação, temas claro/escuro, safe area | `AppFrame` no estado `loading` | Reutilizar em qualquer espera de workspace; não substituir por texto estático |
| `WorkspaceProvider`   | Estado de persistência  | `apps/web/src/lib/features/workspace/WorkspaceProvider.svelte`    | Próprio com `WorkspaceState`                              | Inicializa o workspace uma vez, pede persistência do origin e disponibiliza context                                                                        | loading, unconfigured, permission-needed, ready, error                                                                    | `+layout.svelte`                          | Única inicialização de storage; páginas não reabrem handle isoladamente                            |
| `PermissionRecovery`  | Composição de domínio   | `apps/web/src/lib/features/workspace/PermissionRecovery.svelte`   | Próprio                                                   | Recupera `queryPermission`/`requestPermission` da pasta local sem tratar o workspace como ausente                                                          | Permissão `prompt`/`denied`, cancelamento e nova escolha de pasta                                                         | `AppFrame`                                | Usar quando o handle existe mas a permissão voltou a `prompt`                                      |
| `ConfigPage`          | Composição de domínio   | `apps/web/src/lib/features/config/ConfigPage.svelte`              | `Tabs`, `WorkspaceSettings`, `BibleLibraryManager`, `WorkspaceStats` | Breadcrumb leve; desktop com abas linha (`Armazenamento`, `Bíblias`, `Estatísticas`, `Lembrete`); mobile com seções empilhadas e rótulos discretos; seção Lembrete diário (switch, horário, permissão, limite iOS); rodapé com versão `APP_VERSION`; sem hero/título grande                                                                 | Tabs com foco visível, landmarks por seção, `prefers-reduced-motion` e painéis acessíveis por teclado                                          | `apps/web/src/routes/config/+page.svelte` | Manter `/config` como rota fina; concentrar layout aqui                                                |
| `Tabs`                | Primitive shadcn-svelte | `apps/web/src/lib/components/ui/tabs/`                            | shadcn-svelte local sobre Bits UI                         | Lista, gatilho e painel com variantes `default` e `line`                                                                                                                                                                   | `role="tablist"`, seleção por teclado, painel associado e foco visível                                                                        | `ConfigPage`, `BibleNoteSplit`             | Usar variante `line` em configurações; reservar `default` para agrupamentos compactos, inclusive Bíblia/Nota no mobile |
| `WorkspaceSettings`   | Composição de domínio   | `apps/web/src/lib/features/workspace/WorkspaceSettings.svelte`    | Próprio                                                   | Prop `embedded` remove cabeçalho hero; mostra tipo, nome, status de Bíblias e persistência; ações de importar, escolher pasta, `persist()` e trocar armazenamento (OPFS/pasta com confirmação e sem migração)                | Pronto, persistido, evictable, erro recuperável                                                                           | `/config` via `ConfigPage`                | Reutilizar para diagnóstico do workspace; não duplicar o onboarding                                |
| `LocalBibleImport` | Composição de domínio | `apps/web/src/lib/features/bible/LocalBibleImport.svelte` | Próprio com `Button` | Props `storage`, `onInstalled`; dropzone, seleção múltipla `.sqlite`, progresso e resultado individual | Vazio, seleção, progresso, parcial, erro com retry, `aria-live`, foco visível | `BibleReader` vazio (modal) | Reutilizar para upload local fora do onboarding; não duplicar dropzone |
| `BibleLibraryManager` | Composição de domínio | `apps/web/src/lib/features/bible/BibleLibraryManager.svelte` | Próprio com `Button`, `Dialog` | Props `storage` opcional; lista versões (nome, arquivo, livros, tamanho), diagnósticos e exclusão com confirmação | Loading, erro com retry, vazio, confirmação com foco preso, resultado em `aria-live` | `/config` aba Bíblias | Reutilizar para gestão de SQLite; não tocar Markdown |
| `WorkspaceStats` | Composição de domínio | `apps/web/src/lib/features/workspace/WorkspaceStats.svelte` | Próprio | Props `storage` opcional; grade de indicadores (Bíblias, notas ativas/lixeira, sermões, bytes) | Loading, erro com retry, zeros com orientação | `/config` aba Estatísticas | Manter cálculo em `workspace-stats.ts`, apresentação aqui |

| `PageHeader`          | Composição compartilhada | `apps/web/src/lib/features/navigation/PageHeader.svelte`           | Próprio                                                   | Props `eyebrow`, `title`, `description` e snippet `actions`; cabeçalho único para lista e detalhe CRUD                                                      | Título em `h1`, ações alinhadas à direita, quebra responsiva em mobile                                                    | `/notes`, `/notes/[id]`                   | Reutilizar em todo CRUD; não duplicar markup de cabeçalho por página                               |
| `NotesList`           | Composição de domínio   | `apps/web/src/lib/features/notes/NotesList.svelte`                | Próprio com `NotesDataTable`, `NoteCardList` e `Dialog`    | Props `storage`, `onOpen`, `onError`; lista notas ativas e move para lixeira com confirmação; criação pertence ao header do shell                          | Loading, vazio, erro com retry, DataTable desktop, cards mobile e `Dialog` de exclusão                                      | `/notes`                                  | Manter o conteúdo da rota dedicado à coleção; filtros futuros ficam junto da tabela                 |
| `NotesDataTable`      | Bloco interno           | `apps/web/src/lib/features/notes/NotesDataTable.svelte`           | Composição local no padrão shadcn-svelte                  | Props `notes`, `onOpen`, `onDelete`; tabela semântica em largura total com colunas `ID`, título, atualização e ações                                        | Caption oculta, cabeçalhos semânticos, foco visível, ações nomeadas; visível a partir de 768 px                              | `NotesList`                               | Estender para ordenação e filtros sem duplicar a coleção desktop                                    |
| `NoteCardList`        | Bloco interno           | `apps/web/src/lib/features/notes/NoteCardList.svelte`             | Próprio                                                   | Props `notes`, `onOpen`, `onDelete` opcional e `hideDelete`; projeção mobile equivalente da coleção; reutilizada no split do leitor com delete oculto                                                                         | Título truncado, ID em Geist Mono, data e ações editar/apagar nomeadas; visível abaixo de 768 px                             | `NotesList`, `BibleNoteSplit`             | Manter paridade de conteúdo e ações com `NotesDataTable`; no leitor só abrir nota                    |
| `MilkdownNoteEditor` | Composição de domínio | `apps/web/src/lib/features/notes/MilkdownNoteEditor.svelte` | Milkdown 7.22.1 (ProseMirror + remark), `milkdown-verse-node.ts`, `milkdown-slash.ts` | Props `note`, `storage`, `onSaved` e `markdown` opcional para preview/teste; edita Markdown direto, autosave 650 ms, nó `:::verse`, slash desktop e drawer mobile 90dvh | Salvando/salvo/erro com `aria-live`; canvas full-bleed sem moldura; slash por setas/Enter/Escape; drawer com título, busca e foco; claro/escuro e reduced motion | `/notes/[id]`, `BibleNoteSplit` | Motor principal das notas; preservar H1↔YAML, snapshot e índice auxiliar; não converter Markdown para HTML como fonte |
| `MilkdownMobileToolbar` | Bloco interno | `apps/web/src/lib/features/notes/MilkdownMobileToolbar.svelte` | Próprio com `Button` shadcn-svelte e Lucide | Props `active`, `activeActions`, `onAction`; ações Negrito, Itálico, Título, Lista, Checklist, Citação e Versículo | `role=toolbar`, nomes acessíveis, `aria-pressed` e estado ativo quando aplicável, alvos de 44 px, rolagem horizontal; fixa acima da barra mobile e respeita safe area | `MilkdownNoteEditor` | Reusar somente em editores Milkdown mobile; `Versículo` abre o mesmo `VerseSelector` |
| `VerseBlockView`      | Bloco interno           | `apps/web/src/lib/features/notes/VerseBlockView.svelte`          | Próprio                                                   | Props de referência (`versionId`, `bookId`, `book`, `chapter`, `verseStart`, `verseEnd`) e `snapshotBody`; callout editorial do snapshot                   | `blockquote` com `aria-label`, referência em mono, corpo serifado, borda lateral sutil sem sombra decorativa                | `NoteCanvasEditor`, `VerseSelector`       | Reutilizar callout em qualquer preview de versículo; não consultar SQLite na leitura               |
| `VerseSelector`       | Composição de domínio   | `apps/web/src/lib/features/notes/VerseSelector.svelte`            | Próprio com `Dialog`, `Sheet`, `Button`, `VerseBlockView` | Props `open` (bindable), `storage`, `onConfirm`, `onCancel`; seletor versão/livro/capítulo/intervalo com preview callout                                    | Dialog desktop, Sheet mobile, catálogo carregando, versão ausente, intervalo inválido, preview disponível                   | `NoteCanvasEditor`                        | Reaproveitar padrão Dialog/Sheet da SPEC-0003; prefill de `readerSelection` sem alterá-lo          |
| `Sheet`               | Primitive shadcn-svelte | `apps/web/src/lib/components/ui/sheet/`                           | shadcn-svelte local sobre Bits UI                         | Drawer lateral ou inferior com portal, overlay, título e fechamento; `side=bottom` ocupa `90dvh` (fallback `90vh`) com `safe-area-inset-bottom` e rolagem interna                                                                                                    | Foco gerenciado, gesto de fechar, `role="dialog"` em mobile                                                                | `VerseSelector`, `BibleReader`            | Usar para fluxos longos no mobile; manter paridade de conteúdo com `Dialog` no desktop              |
| `RemoteBibleImport` | Composição de domínio | `apps/web/src/lib/features/bible-remote/RemoteBibleImport.svelte` | Próprio com `Button`, `Input` shadcn-svelte | Props `storage`, `variant` (onboarding/bible/config), `initialUrl`; campo URL, lista .sqlite com seleção, progresso por arquivo/global e resultado individual | Loading lista, erro recuperável, vazio, pronto, baixando, parcial, sucesso, `aria-live`, progresso com `aria-valuenow`, foco visível, teclado, `prefers-reduced-motion` | `OnboardingModal`, `BibleReader` vazio, `WorkspaceSettings` via `/config` | Reutilizar nas 3 superfícies; não duplicar importador remoto |
| `Empty` | Primitive shadcn-svelte | `apps/web/src/lib/components/ui/empty/` | registry shadcn-svelte (CLI) | `Empty`, `Empty.Header`, `Empty.Media` (icon), `Empty.Title`, `Empty.Description`, `Empty.Content` para estados vazios | Título em heading real quando necessário, foco visível nas ações, `aria-live` no entorno | `BibleReader` vazio | Usar para todo estado vazio com ação; não recriar painel vazio próprio |
| `AlertDialog`         | Primitive shadcn-svelte | `apps/web/src/lib/components/ui/alert-dialog/` (via `Dialog`)     | shadcn-svelte local                                       | Confirmação destrutiva com título, descrição e ações primária/secundária                                                                                   | Foco preso, retorno de foco, botões nomeados                                                                                | `NotesList` (exclusão)                    | Usar para ações irreversíveis; não substituir por `window.confirm`                                 |

## Telas e composição

| Tela ou rota                      | Arquivo                                                                  | Componentes Svelte usados                               | Dados e ações                                                                                                                                                                                                                                                                | Estados                                                                                                           |
| --------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| Início (home operacional)         | `apps/web/src/routes/+page.svelte`                                       | `OnboardingModal`, `HomePage`                           | Carregar configuração, orientar onboarding e exibir a home com Continuar leitura, ações rápidas e recentes reais dentro do shell                                                                                                                                                                                            | Onboarding, home sem redirect, skeleton, vazio, erro com retry e safe area                                        |
| Entrada inicial (removida)        | —                                                                       | —                                                       | Seletor com redirect removido em SPEC-0012; a rota `/` é a home operacional                                                                                                                                                                                                  | Removida                                                                                                          |
| Configuração                      | `apps/web/src/routes/config/+page.svelte`                                | `ConfigPage`, `Tabs`, `WorkspaceSettings` | `h1` acessível Configurações sem hero; abas no desktop e seções no mobile; armazenamento, persistência, importação e pasta sem hero                                                                                                                | Pronto, persistido, evictable, erro, sem escolha e sucesso                                          |
| Bíblia                            | `apps/web/src/routes/bible/+page.svelte`                                 | `BibleReader`, `VerseNoteSelector`, `HighlightsList`, `SelectionActionPopover`, `BibleNoteSplit`, `NoteCardList`, `ButtonGroup`, `Dialog`, `Sheet`, `Tabs` | `h1` acessível Bíblia na rota; barra em pílula (navegação, versão, busca, **Destaques**); sheet workspace-wide de destaques; ícone de nota (1 nota → split direto; 2+ → seletor com **Ver todas** → cards filtrados); selecionar livro/capítulo, pesquisar, ler, destacar, copiar e criar nota | Loading, vazio com CTA de importação, erro parcial/total, capítulo vazio, sucesso, busca, sheet Destaques, ícone/seletor multi-nota, seleção, popover, split/abas/cards, clipboard/workspace e limites |
| Destaques                         | `apps/web/src/routes/highlights/+page.svelte`                            | `AppFrame`, `HighlightsList`                            | Consulta workspace-wide de `reader_highlight` com o mesmo conjunto do sheet do leitor; título **Destaques** no shell e na página                                                                                      | Loading, vazio observável, erro recuperável, workspace indisponível                                               |
| Sermões                           | `apps/web/src/routes/sermons/+page.svelte`                               | `ProductPage`                                           | Reservar a superfície do construtor futuro                                                                                                                                                                                                                                   | Estado inicial do módulo                                                                                          |
| Estudos                           | `apps/web/src/routes/study/+page.svelte`                                 | `ProductPage`                                           | Comunicar disponibilidade futura                                                                                                                                                                                                                                             | Estado explícito `Em breve`                                                                                       |
| PWA e instalação                  | `apps/web/static/manifest.webmanifest`, `apps/web/src/service-worker.ts` | Manifesto, app shell e service worker                   | Instalação standalone, viewport com zoom travado (`maximum-scale=1.0, user-scalable=no`), campos em 16px no touch contra o zoom de foco do iOS, cache versionado e fallback offline das rotas locais (`/`, `/bible`, `/notes`, `/highlights`, `/sermons`, `/study`, `/config`)                                                                                                                                                                                            | Primeiro carregamento online, cache disponível e indisponibilidade de rede                                        |
| Biblioteca de estudos (planejada) | A definir                                                                | Componentes Svelte a definir                            | Consultar, criar, editar e apagar estudos e sermões                                                                                                                                                                                                                          | A definir                                                                                                         |
| Construtor de sermões (planejado) | A definir                                                                | Componentes Svelte a definir                            | Estruturar, editar e salvar sermões                                                                                                                                                                                                                                          | A definir                                                                                                         |
| Leitor da Bíblia                  | `apps/web/src/lib/features/bible/BibleReader.svelte`                     | `BibleReader`, `BibleReaderControls`, `BibleVerseList`, `HighlightsList`, `SelectionActionPopover`, `BibleNoteSplit` | Consultar texto SQLite OpenLP importado, navegar, buscar, abrir sheet de destaques workspace-wide, ver ícone de nota no versículo, selecionar intervalo, destacar, copiar e abrir nota em split/abas                                                                                                                                 | Loading, indisponível, vazio, erro, parcial, sucesso, busca, sheet Destaques, ícone de nota, seleção, popover, split/abas e limites                                             |
| Notas — listagem                  | `apps/web/src/routes/notes/+page.svelte`                                 | `AppFrame`, `PageHeader`, `NotesList`, `NotesDataTable`, `NoteCardList`, `Dialog` | `PageHeader` “Notas” com ação “Nova nota” só no mobile (desktop usa o header do shell); o conteúdo lista `notes/`, abre editor e move para `trash/` com confirmação; DataTable no desktop e cards no mobile                                                                                                                     | Loading, vazio, erro com retry, workspace indisponível, tabela e cards equivalentes                               |
| Notas — editor canvas             | `apps/web/src/routes/notes/[id]/+page.svelte`                            | `AppFrame`, `MilkdownNoteEditor`, `MilkdownMobileToolbar`, `VerseSelector`, Milkdown | Editar Markdown direto em toda a área útil, renderizar CommonMark/GFM, normalizar H1↔YAML, inserir `:::verse` por slash/drawer/toolbar e preservar referência + snapshot no roundtrip | Carregando, nota não encontrada, salvando/salvo/erro, slash desktop ancorado ao cursor, drawer 90dvh, toolbar mobile com estados pressed, seletor aberto, callout restaurado |

### Onboarding de configuração e armazenamento

| Bloco               | Tipo                  | Arquivo                                                       | Origem                         | Finalidade e API pública                                                                                                                                     | Estados e acessibilidade                                                                                                                                     | Consumidores                       | Reaproveitar ou estender                                                                           |
| ------------------- | --------------------- | ------------------------------------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------- | -------------------------------------------------------------------------------------------------- |
| `OnboardingModal`   | Composição de domínio | `apps/web/src/lib/features/onboarding/OnboardingModal.svelte` | Próprio, com elementos nativos | Props `storageMode`, `storage`, `initialError`, `initialStep`, `onChooseStorage`, `onChooseBrowserStorage`, `onComplete` e `onDeferred`; pode abrir diretamente na etapa de importação | Intro, escolha, instalação, importação, erro com ação explícita de fallback OPFS, parcial e sucesso; `role=dialog`, foco visível, `aria-live`, trap de Tab e ações desabilitadas durante escrita | `apps/web/src/routes/+page.svelte` | Reutilizar a etapa de importação a partir do estado vazio do leitor sem duplicar upload ou storage; manter OPFS como alternativa somente após falha local |
| `ProgressIndicator` | Bloco interno         | `apps/web/src/lib/features/onboarding/OnboardingModal.svelte` | Elemento nativo `<progress>`   | Mostra progresso percentual da operação atual                                                                                                                | `aria-valuemin`, `aria-valuemax`, `aria-valuenow`, texto percentual e estado de operação                                                                     | `OnboardingModal`                  | Extrair somente quando outra operação assíncrona precisar do mesmo contrato                        |
| `BibleDropzone`     | Bloco interno         | `apps/web/src/lib/features/onboarding/OnboardingModal.svelte` | Input nativo e drag-and-drop   | Seleciona múltiplos arquivos `.sqlite` e encaminha arquivos escolhidos/soltos                                                                                | Operável por clique, Enter e Espaço; seleção vazia e resultados inválidos/parciais são anunciados                                                            | `OnboardingModal`                  | Extrair para um upload local compartilhado quando houver segundo consumidor                        |

## Regras de composição

1. Páginas e rotas Svelte coordenam dados e compõem componentes; não concentram a
   grade, formulário, filtros, overlays ou cartões reutilizáveis.
2. Antes de criar um componente, consulte esta tabela e reaproveite o item
   existente quando ele atender à mesma intenção.
3. Qualquer primitive ou composição adicionada entra na tabela com seu arquivo,
   origem, explicação, API, estados e consumidores reais.
4. shadcn-svelte está configurado em `apps/web/components.json`; registrar
   cada primitive somente quando existir no projeto e tiver consumidor real.
5. Para dashboards, registre a pergunta operacional, escopo, filtros,
   indicadores, visualizações, tabela de investigação e estados. Prefira
   blocos existentes e primitives shadcn-svelte antes de criar uma nova
   composição.
6. Em CRUD, reutilize o mesmo `PageHeader` nas telas de lista, detalhe,
   criação e edição. Registre uma única implementação, suas props, variações e
   consumidores; não duplique markup de cabeçalho por página.
7. Linhas de `DataGrid` com detalhe usam link acessível em toda a área; a
   listagem ocupa a largura disponível, exibe a coluna `ID` e oferece ações de
   editar e apagar. Ações internas usam `TableRowAction` ou equivalente e não
   propagam a navegação.
8. Formulários de criar e editar usam seções com coluna de contexto e painel
   em duas colunas nos breakpoints largos, refluindo para uma no mobile.
9. Toda tela renderiza `Breadcrumb` com o nome da equipe ativa, o módulo e o
   título atual. Em Laravel, reaproveite o `Breadcrumb` ou `Breadcrumbs` do
   layout existente e registre o componente real em vez de criar outro.
10. Ao criar um bloco, registre-o nesta tabela na mesma tarefa. Ao alterar ou
    remover um bloco, atualize seus consumidores e a orientação de reuso.
11. Superfícies de navegação e itens de escolha começam transparentes; o hover
    pode usar `var(--muted)` ou `var(--sidebar-accent)`, enquanto seleção e foco
    usam borda, texto e semântica sem depender apenas de cor.
12. Use Geist Sans para a interface e Geist Mono somente para código, caminhos e
    identificadores técnicos. Não introduza decoração para preencher espaço.

<!-- markdownlint-enable MD013 -->
